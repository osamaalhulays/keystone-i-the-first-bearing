# Keystone I: The First Bearing

## Bridge-Admitted Response Unification in Deep Origin Theory

Author line to be finalized

Date: 2026-04-29

## Abstract

We present Deep Origin Theory, in its Keystone I line, as a governed retarded-response unification framework linking a south finite-volume traceless response object and a north same-cluster traceless response object through a source-quality common response manifest, a source-governed common test basis, and a no-smoothing distributional basis harmonization map. The south object is authorized as a source-quality traceless response object below kernel under a nonpromoting ceiling, while the north object is authorized as a source-quality traceless response object below Gate2. These independently authorized objects are brought into a common same-parent response setting without operator surplus, parameter split, kernel promotion, or Gate2 promotion.

The resulting same-parent traceless pair survives bridge engineering review and bridge admission on an authorized common test basis. The accepted bridge layer uses an exact distributional embedding for the south object, signed cell integration for the north object, zero projection debt, zero smoothing, and positive robust overlap diagnostics with canonical values `Xi_raw = 0.3148145037995458`, `Xi_hat = 0.7589533617835368`, and `Xi_hat_min = 0.751178834903882`. In the program's internal governance record, this bridge-supported common-response layer proceeds under an explicit `NO_COSMOLOGY_COMPLETION` ceiling.

This paper does not claim cosmology completion, a predictive `H(z)` law, a `D_L(z)` model vector, Pantheon+ agreement, CMB completion, kernel promotion, or a final public Theory-of-Everything result. Instead, it presents the externally reviewable content at the bounded level warranted by the current evidence: a bridge-admitted response-unification framework whose cosmology program remains open and begins with a separate primitive-freeze stage.

## 1. Introduction

The problem addressed in this paper is narrower and more technical than a generic “Theory of Everything” narrative. We ask whether one can lawfully connect a microscopic retarded-response object and a macroscopic same-cluster response object inside one governed response architecture without cheating through smoothing, hidden sector parameters, operator surplus, or an uncontrolled change of parent object.

In the program's internal language, the relevant fronts were historically separated. The south front matured into a finite-volume traceless response object below kernel. The north front matured into a same-cluster traceless response object below Gate2. These two fronts were then brought into contact through a common response manifest, a common test basis, and a bridge-admission pipeline whose engineering layer explicitly rejected earlier basis-map artifacts before accepting the final no-smoothing rerun.

The result is publishable now, but only under a strict ceiling. We therefore do not present this paper as completed cosmology. We also do not present it as a final public Theory-of-Everything announcement. What we do present is a response-unification framework with a bridge-admitted north-south pair, together with the engineering tests, falsifiers, and boundedness conditions that make this claim reviewable.

## 2. Executive Claim and Scope

The externally reviewable claim of this paper is:

> Deep Origin Theory, in its Keystone I line, provides a governed common-response framework in which independently authorized south and north traceless response objects can be harmonized through a source-governed common test basis, yielding a bridge-admitted same-parent layer without operator surplus, parameter split, kernel promotion, or Gate2 promotion.

The paper is intentionally bounded by the following non-claims:

- no completed cosmology
- no final public TOE claim
- no `H(z)` prediction
- no `D_L(z)` or `m_model(z)` model vector
- no Pantheon+ fit
- no BAO/CMB closure
- no perturbation closure
- no CMB Boltzmann completion

The internal governance record does treat the present stage as bounded by `NO_COSMOLOGY_COMPLETION`. We state this only to explain the internal ladder. The paper does not ask the reader to accept that ladder as peer review. The reader is asked to evaluate the response-unification content directly.

## 3. Formal Objects and Notation

The common traceless projector is written as

`Pi_0(A) = A - (Tr(A)/M) I_M`

with the relevant support dimension specified per sector.

The south response object is

`R_S^circ = Pi_0(K_R^{Theta_S} - P_ThetaS)`

with south support dimension `63`.

The north response object is

`R_N^circ_local = Pi_0(K_N - P_N)`

with north support dimension `65`.

The governing same-parent declaration used in the admitted common-response layer is

`O_phi = T_m^ren`.

This same-parent declaration matters because the goal is not to place two unrelated arrays side-by-side, but to show that their descent to a shared response parent is lawful and remains lawful through harmonization and bridge review.

## 4. South Sector: Authorized Traceless Response Below Kernel

The south front is closed inside its own scope. The authoritative south packet declares

- `SOUTH_AUTHORITY_ACCEPT_NONPROMOTING_R_S_CIRC_BELOW_KERNEL`
- `SOURCE_QUALITY_SOUTH_TRACELESS_RESPONSE_OBJECT_BELOW_KERNEL`

and binds the object

`R_S^circ = Pi_0(K_R^{Theta_S} - P_ThetaS)`

under the ceiling

- south-only
- below-kernel
- nonpromoting

The south authorization also fixes the following:

- `canonical_review_pass = 7/7`
- `arrival_cycle_closed = true`
- `contact_is_local = true`
- `physical_cut_discontinuity = 0`
- `absorbs_nonlocal_spectral_debt = false`

This means that the south front is not a live blocker for publication. It is already closed inside pure south scope, and the package used here does not reopen row-count questions, south hunts, or any same-object authority loop on the already authorized response object.

Just as importantly, the south object is not promoted beyond its lawful ceiling. In particular:

- no `rho_S` admission
- no `RePi_R` admission
- no `Pi_R` admission
- no denominator admission
- no kernel admission

The present paper preserves that ceiling exactly.

## 5. North Sector: Source-Quality Traceless Response Below Gate2

The north front progressed from a local non-admitting object to an authorized source-quality object below Gate2. The governing north packet declares

- `NORTH_SOURCE_QUALITY_AUTHORITY_SUPPLIED_FOR_CURRENT_LOCAL_OBJECT`

over a local object of type

- `radial_binned_traceless_response_contrast_local_object`

with

- `bin_count = 65`
- `traceless_survival = 65/65`
- `positive_bins = 21`
- `negative_bins = 44`

The key point is that the positive/negative bin split is not used here as a spectral-density positivity claim. It is explicitly governed as traceless contrast around the mean in the north native space. That distinction matters because the north object is not being smuggled in as a transport-spectrum positivity theorem. It is being used as a same-cluster traceless response object with source-quality authority below Gate2.

The north ceiling remains bounded:

- no Gate2 flip
- no standalone `Gamma_s` admission
- no overlap claim by north alone
- no bridge claim by north alone

As with the south, this paper keeps the north front exactly inside that ceiling.

## 6. Common Response Manifest and Same-Parent Setting

The common manifest layer declares a source-quality common response object below pair with the following decisive features:

- same parent declared
- same primitive set declared
- no operator surplus
- no parameter split
- contact/subtraction discipline preserved

The manifest binds the authorized south and north objects into one same-parent common-response setting:

- south status: `SOURCE_QUALITY_SOUTH_TRACELESS_RESPONSE_OBJECT_BELOW_KERNEL`
- north status: `SOURCE_QUALITY_NORTH_R_N_CIRC_OBJECT_BELOW_GATE2`
- common parent: `O_phi = T_m^ren`

This stage is crucial because it prevents the bridge from being interpreted as a numerical coincidence between unrelated objects. The bridge review later tests a pair that is already declared as same-parent inside a governed common-response manifest.

## 7. Basis Harmonization: Why the Final Admitted Layer Uses No Smoothing

The publication-level bridge claim does not rest on the historical pre-bridge surrogate basis layer. Earlier in the workflow, a pre-overlap harmonization map was constructed using a shape-only surrogate layer on a common coordinate. That layer was useful operationally, but it was not accepted as the final authority basis for bridge engineering.

The decisive publication layer is the final bridge basis-map rerun:

- `basis_map_v1_class = DISTRIBUTIONAL_PAIRING_MAP_NO_SMOOTHING`
- `common_space_status = defined__source_governed_common_test_basis`
- `H_S_status = pass__exact_distributional_embedding`
- `H_N_status = pass__signed_cell_integration_on_authorized_common_test_basis`
- `width_required = false`
- `width_authority_status = not_required__distributional_pairing_no_smoothing`

This matters for two reasons.

First, it removes the most obvious line of attack against a weak bridge claim: that positive overlap was created by an arbitrary smoothing width, a Lorentzian fit, or a post-hoc rescaling.

Second, it makes the admitted layer easier to falsify. If the bridge only survived because one smoothed away the mismatch between south and north supports, then it should fail when smoothing is removed. The admitted rerun shows the opposite: the bridge survives the no-smoothing authority layer and does so with zero recorded loss and zero projection debt.

The common test basis itself is explicitly finite and source-governed. The rerun notes a five-anchor common test basis on the normalized common coordinate. The south object is embedded exactly on this basis. The north object is paired by signed cell integration on the same basis. The framework therefore claims a lawful common-response test space, not a vague matching heuristic.

## 8. Same-Parent Traceless Pair and Bridge Engineering

The pair-level engineering tests are the heart of the paper because they convert a conceptual common-response story into a reviewable acceptance structure.

The bridge rerun reports:

- `loss_S = 0.0`
- `loss_N = 0.0`
- `projection_debt = 0.0`
- `Xi_raw = 0.3148145037995458`
- `Xi_hat = 0.7589533617835368`
- `Xi_hat_min = 0.751178834903882`

and the following engineering verdict:

- `BRIDGE_CANDIDATE_ENGINEERING_REVIEW_PASS__NO_UNIFICATION`

with detailed test results:

- `same_parent_descent_consistency = pass`
- `basis_map_consistency = pass`
- `trace_null_compatibility = pass`
- `contact_subtraction_compatibility = pass`
- `unit_sign_compatibility = pass`
- `Xi_robustness = pass`
- `basis_map_loss_projection_debt = pass`
- `operator_surplus = none`
- `parameter_split = none`
- `overclaim_guard = pass`

The robustness condition is especially important. The paper does not ask the reader to accept `Xi > 0` in isolation. The engineering layer requires positivity under a declared robustness envelope. The rerun passes that test through `Xi_hat_min > 0` under interior Voronoi boundary shifts, with no smoothing and no zero-padding.

## 9. Bridge Admission and Unified Response Layer

Once the engineering layer passed, the framework advanced to bridge admission:

- `BRIDGE_ADMISSION_ACCEPTED__NO_UNIFICATION`

This stage preserves the following ceiling:

- bridge admitted
- unification not yet automatically opened by bridge alone
- no cosmology completion
- no kernel promotion
- no Gate2 flip

The framework then proceeded through a separate unification adjudication:

- `UNIFICATION_ADJUDICATION_ACCEPTED__NO_TOE`

and later to the higher internal stage:

- `TOE_ADJUDICATION_ACCEPTED__NO_COSMOLOGY_COMPLETION`

For the purposes of this paper, the critical point is simple: the bridge layer is not hypothetical. It is admitted inside the governed response architecture, and the positive bridge verdict is carried by a lawful engineering layer rather than by rhetoric, smoothing, or relaxed loss accounting.

## 10. Governance, Scope, and Why the Ceiling Is a Scientific Asset

The program uses a strong internal governance stack. In other contexts this might sound bureaucratic, but here it plays a scientific role: it prevents the paper from claiming more than the artifacts support.

The relevant status table is:

| Layer | Status in this paper |
| --- | --- |
| South `R_S^circ` | accepted below kernel |
| North `R_N^circ` | source-quality authority supplied below Gate2 |
| Common response manifest | present |
| Common test basis | present |
| Basis harmonization | admitted in no-smoothing distributional form |
| Same-parent pair | present |
| Bridge | admitted |
| Unification | internally accepted |
| TOE-grade internal stage | accepted under no cosmology completion |
| Cosmology completion | held closed |
| Kernel promotion | false |
| Gate2 flip | false |

This ceiling is not cosmetic. It protects the paper from collapsing into an overclaim. The present contribution is substantial precisely because it is bounded. A bridge-admitted response-unification layer with explicit kill criteria is easier to evaluate, easier to falsify, and more durable under hostile review than a premature totalizing announcement.

## 11. Falsifiers

The paper is meant to be killable if its central claim is wrong. The most direct falsifiers are:

`F1.` If the common-response bridge requires Lorentzian or Gaussian smoothing as authority, the present bridge claim fails.

`F2.` If a hidden sector-specific parameter, width, coupling, amplitude, or normalization must be introduced after harmonization, the no-parameter-split condition fails.

`F3.` If tracelessness is not preserved in the declared common test basis, the same-parent pair is invalid.

`F4.` If contact/subtraction terms create or hide the overlap signal, the bridge claim fails.

`F5.` If the accepted overlap survives only by discarding decisive nonmatching support, then the zero-loss / zero-projection-debt result is false and the bridge claim fails.

`F6.` If the future cosmology program requires residual fitting of `H0`, `M`, `alpha`, `gamma`, or initial scalar data in order to generate the first lawful background model vector, then the current framework may remain a bridge-admitted response theory while the complete-TOE cosmology program fails.

The key point is that the present paper already specifies how it could be wrong.

## 12. What This Paper Does Not Claim

This paper does not claim:

- No H(z) prediction yet.
- No D_L(z) or m_model(z) yet.
- No Pantheon+ fit.
- No CMB completion.
- No the live cosmology tail complete TOE claim.
- a global cosmological law for `A(phi)` or `alpha(phi)`
- a frozen scalar potential `V(phi)`
- a frozen `gamma_GB` policy
- a frame-complete cosmological observable dictionary
- frozen cosmological initial conditions
- a lawful `H(z)` solver
- a lawful `D_L(z)` or `m_model(z)` vector
- Pantheon+ success
- BAO/CMB background closure
- perturbation closure
- CMB Boltzmann spectra
- inflationary closure
- complete cosmology

These are not omissions hidden in the fine print. They are explicitly deferred to the the live cosmology tail cosmology program.

## 13. the live cosmology tail Cosmology Program

The next live front is cosmology completion only. The framework is already canonically stabilized at Keystone I, and the next lawful packet is

- `the separate primitive-freeze cosmology program`

The first exact blocker named in the handoff is

- `global_cosmology_law_for_A_phi_or_alpha_phi`

The remaining the live cosmology tail degrees, in canonical order, are:

1. primitive freeze
2. background field equations
3. background solver
4. no-fit distance/model vector
5. Pantheon+ covariance evaluation
6. BAO/CMB background distances
7. linear perturbations and growth
8. CMB Boltzmann implementation
9. initial-conditions or inflation packet
10. falsifiers, reproducibility, and final cosmology adjudication

The present paper is therefore best understood as the pre-cosmology publication of the response-unification architecture, not as the cosmology paper itself.

## 14. Reproducibility and Artifact Inventory

The present package is backed by explicit artifact paths rather than by narrative summary alone.

The essential reviewer-facing artifacts are:

- south sovereign authorization
- north source-quality authority
- common response manifest
- final no-smoothing basis-map rerun
- bridge engineering test rows
- bridge admission acceptance
- unification acceptance
- TOE acceptance under no cosmology completion
- cross-workspace canonical sync

The reproducibility bundle in this package lists these files explicitly and records the canonical engineering values used in the manuscript.

## 15. Response-Language Anchors

The response language used in this manuscript was not chosen arbitrarily. It is aligned with source families internally adjudicated as legitimate anchors for the retarded-response vocabulary, including:

- `HATTORI_ITAKURA_2022_POLARIZATION_TENSOR_I_2205_04312`
- `HATTORI_ITAKURA_2022_POLARIZATION_TENSOR_II_2205_06411`
- `BRAHMA_SCHUTZ_2024_RETARDED_SELF_ENERGY_2410_14771`

These anchors support language such as:

- the retarded polarization tensor as a linear response function
- retarded prescription `omega -> omega + i epsilon`
- retarded self-energy as linear response of the medium
- induced-current chains of the form `J_ind = -Pi_R A`

They do not, by themselves, establish the broader Deep Origin Theory mission. They justify the response vocabulary used in the formal sectors of this paper.

## 16. Conclusion

Keystone I is publishable now, but not as completed cosmology and not as a final public Theory-of-Everything claim. What is publishable now is narrower and, in practice, stronger: a bridge-admitted response-unification framework whose microscopic and macroscopic sectors are lawfully connected through a same-parent common-response architecture and a no-smoothing common test basis.

The paper is therefore meant to stand on three legs:

1. independently authorized south and north traceless response objects
2. a governed common-response and bridge-admission architecture
3. an explicit ceiling that holds cosmology closed until the primitive-freeze program is completed

This combination is sufficient for a serious first paper. It establishes precedence, protects the claim against easy overreach, and leaves the cosmology program to be opened where it should be opened: at primitives, not at fitted observables.

## Appendix A. Reviewer-Facing Status Grammar

- south: `SOURCE_QUALITY_SOUTH_TRACELESS_RESPONSE_OBJECT_BELOW_KERNEL`
- north: `NORTH_SOURCE_QUALITY_AUTHORITY_SUPPLIED_FOR_CURRENT_LOCAL_OBJECT`
- bridge: `BRIDGE_ADMISSION_ACCEPTED__NO_UNIFICATION`
- internal unification: `UNIFICATION_ADJUDICATION_ACCEPTED__NO_TOE`
- internal toe-grade stage: `TOE_ADJUDICATION_ACCEPTED__NO_COSMOLOGY_COMPLETION`

Public rendering rule:

- talk publicly at the bridge-admitted response-unification level
- keep cosmology held closed
- do not promote the internal toe-grade label to a final public claim

## Appendix B. Canonical Engineering Numbers

- `basis_map_v1_class = DISTRIBUTIONAL_PAIRING_MAP_NO_SMOOTHING`
- `loss_S = 0.0`
- `loss_N = 0.0`
- `projection_debt = 0.0`
- `Xi_raw = 0.3148145037995458`
- `Xi_hat = 0.7589533617835368`
- `Xi_hat_min = 0.751178834903882`

## Appendix C. Public One-Sentence Summary

> We publish Keystone I as a bridge-admitted response-unification framework with cosmology explicitly held closed pending a separate primitive-freeze cosmology program.
